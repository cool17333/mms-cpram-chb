# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
server/.env

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Exported reports (generated files)
BD_*.pdf
BD_*.png

# เอกสาร/ — เอกสาร/ข้อมูล/อ้างอิง (ไม่ใช่โปรแกรม) เก็บ local เท่านั้น
# ยกเว้น plan/ ที่ยังแชร์ผ่าน git ได้
# หมายเหตุ: ใช้ "เอกสาร/*" (ไม่ใช่ "เอกสาร/") เพื่อให้ git ยัง descend เข้าไป re-include plan/ ได้
เอกสาร/*
!เอกสาร/plan/

# graphify-out/ — output ของ skill
graphify-out/
